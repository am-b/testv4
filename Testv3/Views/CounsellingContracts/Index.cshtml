@model IEnumerable<Testv3.Models.CounsellingContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CompletionDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Counsellor.CounsellorID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.StudentID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CompletionDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Counsellor.CounsellorID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.StudentID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CounsellingContractID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CounsellingContractID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CounsellingContractID })
        </td>
    </tr>
}

</table>
