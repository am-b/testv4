@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()
        <ul class="nav navbar-nav">
            <li class="dropdown user user-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <img src="~/admin-lte/dist/img/avatar2.png" class="user-image" alt="User Image">
                    <span class="hidden-xs">@ViewBag.CurrentUser</span>
                </a>
                <ul class="dropdown-menu">
                    <li class="user-header">
                        <img src="~/admin-lte/dist/img/avatar2.png" class="img-circle" alt="User Image">
                        <p>
                            @ViewBag.CurrentUser
                            <small>@ViewBag.CurrentUserStudentID</small>
                        </p>
                    </li>
                    <li class="user-footer">
                        <div class="pull-left">
                            @Html.ActionLink("Change password", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "btn btn-default btn-flat" })
                        </div>
                        <div class="pull-right">
                            <a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-default btn-flat">Log off</a>
                        </div>
                    </li>
                </ul>
            </li>
            <li>
                <a href="#"></a>
            </li>
        </ul>
    }
}
            @section Scripts {
                @Scripts.Render("~/bundles/jqueryval")
                @Scripts.Render("~/bundles/jquery")
                @Scripts.Render("~/bundles/bootstrap")
                @Scripts.Render("~/admin-lte/js")
            }

